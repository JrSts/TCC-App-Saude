const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
  container:{
    flex: 1,
  },

  content: {
    flex: 11,
  },

  img: {
    top: -50,
    left: 15,
    position: "absolute",
  }

})

export default styles