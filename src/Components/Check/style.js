import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  borda: {
    borderWidth: 1,
    borderRadius:50,
    marginRight: 15,
    marginVertical: 2
  }
})

export default styles